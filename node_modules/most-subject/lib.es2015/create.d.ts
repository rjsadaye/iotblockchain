import { Stream } from '@most/types';
import { Subject } from './types';
export declare function create<A = any>(): Subject<A, A>;
export declare function create<A, B>(f: (stream: Stream<A>) => Stream<B>): Subject<A, B>;

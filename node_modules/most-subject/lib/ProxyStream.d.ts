import { Disposable, Scheduler, Sink, Stream, Time } from '@most/types';
import { MulticastSource } from '@most/core';
export declare class ProxyStream<A> extends MulticastSource<A> {
    attached: boolean;
    running: boolean;
    scheduler: Scheduler;
    constructor();
    run(sink: Sink<A>, scheduler: Scheduler): Disposable;
    attach(stream: Stream<A>): Stream<A>;
    error(time: Time, error: Error): void;
    end(time: number): void;
    private cleanup();
}

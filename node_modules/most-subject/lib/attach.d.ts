import { AttachSink } from './types';
import { Stream } from '@most/types';
export declare const attach: AttachFn;
export declare type AttachFn = {
    <A>(sink: AttachSink<A>, stream: Stream<A>): Stream<A>;
    <A>(sink: AttachSink<A>): (stream: Stream<A>) => Stream<A>;
};
